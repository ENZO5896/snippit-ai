{
  "name": "snippit-ai",
  "version": "1.0.0",
  "description": "SNIPPIT AI - A simple AI chat interface",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static HTML - no build required'",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  },
  "devDependencies": {}
}
